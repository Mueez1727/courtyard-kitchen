export default function Loading() {
  return (
    <div className="flex min-h-[50vh] items-center justify-center bg-parchment pt-28">
      <p className="text-sm uppercase tracking-[0.28em] text-muted">Loading…</p>
    </div>
  );
}
